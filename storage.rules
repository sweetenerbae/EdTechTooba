rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /ocr_uploads/{uid}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == uid;
      allow read: if false; // не раздаём фото напрямую
    }
  }
}
